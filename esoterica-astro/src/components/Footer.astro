---
---

<footer class="es-ft-root">

  <!-- Atmosphere strip -->
  <div class="es-ft-strip">
    <span class="es-ft-strip-text">✦ Chiclayo, Perú</span>
    <span class="es-ft-strip-dot">·</span>
    <span class="es-ft-strip-italic">Where the veil grows thin</span>
    <span class="es-ft-strip-dot">·</span>
    <span class="es-ft-strip-text">Est. 2024 ✦</span>
  </div>

  <!-- Main body -->
  <div class="es-ft-body">

    <!-- Brand bar (full-width centered identity) -->
    <div class="es-ft-brand">
      <div class="es-ft-logo">
        <picture>
          <source type="image/webp" srcset="/logo/logo-48.webp 1x, /logo/logo-96.webp 2x" />
          <img src="/logo/logo-48.png" srcset="/logo/logo-48.png 1x, /logo/logo-96.png 2x" alt="Esoterica" width="44" height="44" loading="lazy" />
        </picture>
        <span class="es-ft-logo-name">Esoterica</span>
      </div>
      <p class="es-ft-desc">Crystals, tarot, and ritual arts.<br/>A threshold, not just a shop.</p>
    </div>

    <!-- Ornamental rule with center glyph -->
    <div class="es-ft-brand-rule" aria-hidden="true"></div>

    <!-- Two row groups (mathematically aligned since they're grid siblings) -->
    <div class="es-ft-cols">

      <nav class="es-ft-group" aria-label="Legal">
        <div class="es-ft-group-head">
          <span class="es-ft-group-label">Legal</span>
          <span class="es-ft-group-count" aria-hidden="true">/ III</span>
        </div>
        <a href="/privacy" class="es-ft-row es-ft-row--legal">
          <span class="es-ft-row-num">I</span>
          <span class="es-ft-row-label">Privacy Policy</span>
          <span class="es-ft-row-arrow">↗</span>
        </a>
        <a href="/terms" class="es-ft-row es-ft-row--legal">
          <span class="es-ft-row-num">II</span>
          <span class="es-ft-row-label">Terms of Use</span>
          <span class="es-ft-row-arrow">↗</span>
        </a>
        <a href="/contact" class="es-ft-row es-ft-row--legal">
          <span class="es-ft-row-num">III</span>
          <span class="es-ft-row-label">Contact</span>
          <span class="es-ft-row-arrow">↗</span>
        </a>
      </nav>

      <nav class="es-ft-group" aria-label="Social">
        <div class="es-ft-group-head">
          <span class="es-ft-group-label">Find Us</span>
          <span class="es-ft-group-count" aria-hidden="true">/ III</span>
        </div>
        <a href="https://www.instagram.com/esoterica.cix/" target="_blank" rel="noopener" class="es-ft-row">
          <span class="es-ft-row-num">I</span>
          <span class="es-ft-row-label">Instagram</span>
          <span class="es-ft-row-arrow">↗</span>
        </a>
        <a href="https://www.tiktok.com/@esoterica.cix" target="_blank" rel="noopener" class="es-ft-row">
          <span class="es-ft-row-num">II</span>
          <span class="es-ft-row-label">TikTok</span>
          <span class="es-ft-row-arrow">↗</span>
        </a>
        <a href="https://pinterest.com/esoterica" target="_blank" rel="noopener" class="es-ft-row">
          <span class="es-ft-row-num">III</span>
          <span class="es-ft-row-label">Pinterest</span>
          <span class="es-ft-row-arrow">↗</span>
        </a>
      </nav>

    </div>

    <!-- Direct line: email pulled out of social rows, elevated -->
    <div class="es-ft-direct">
      <span class="es-ft-direct-label">Direct Line</span>
      <a href="mailto:commune@esoterica.com" class="es-ft-direct-link">
        <span class="es-ft-direct-icon" aria-hidden="true">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="5" width="18" height="14" rx="2"></rect>
            <path d="M3 7l9 6 9-6"></path>
          </svg>
        </span>
        commune@esoterica.com
      </a>
    </div>

    <!-- Close: copyright anchor -->
    <div class="es-ft-close">
      <p class="es-ft-close-text">© 2026 Esoterica · All rights reserved</p>
    </div>

  </div>

  <!-- Full-width wordmark: starfall settles into outline text -->
  <div class="es-ft-wordmark-wrap">
    <canvas class="es-ft-starfall" aria-hidden="true"></canvas>
    <div class="es-ft-wordmark es-ft-wordmark--hidden" aria-hidden="true">ESOTERICA</div>
  </div>

</footer>

<script is:inline>
(function() {
  if (!window.matchMedia('(pointer: fine)').matches) return;
  if (!window.gsap) return;
  document.querySelectorAll('.es-ft-row').forEach(function(row) {
    var label = row.querySelector('.es-ft-row-label');
    row.addEventListener('mousemove', function(e) {
      var rect = row.getBoundingClientRect();
      var x = ((e.clientX - rect.left) / rect.width - 0.5) * 10;
      gsap.to(label, { x: x, duration: 0.4, ease: 'power2.out' });
    });
    row.addEventListener('mouseleave', function() {
      gsap.to(label, { x: 0, duration: 0.5, ease: 'power2.out' });
    });
  });
})();
</script>

<script is:inline>
(function() {
  var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  // Color tokens (matches --es-gold-rgb and --es-text-rgb)
  var goldR = 212, goldG = 168, goldB = 83;
  var textR = 232, textG = 224, textB = 212;

  var canvas, ctx, wrap, wordmark;
  var W, H, dpr;
  var stars = [];
  var lines = [];
  var lineAlpha = 0;
  var lineAlphaTarget = 0.32;
  var lineAlphaProxy = { v: 0 };
  var rafId = null;
  var animationStarted = false;
  var hasSettled = false;
  var observer = null;
  var resizeTimer = null;
  var time = 0;

  function cancelLoop() {
    if (rafId) {
      cancelAnimationFrame(rafId);
      rafId = null;
    }
  }

  function teardown() {
    cancelLoop();
    if (typeof gsap !== 'undefined') {
      if (stars.length) stars.forEach(function(s) { gsap.killTweensOf(s); });
      gsap.killTweensOf(lineAlphaProxy);
    }
    if (observer) {
      observer.disconnect();
      observer = null;
    }
    stars = [];
    lines = [];
    lineAlpha = 0;
    lineAlphaProxy.v = 0;
    animationStarted = false;
    hasSettled = false;
  }

  // Hand-authored letter polylines (normalized to a 1.0 unit height per letter).
  // Each entry has `width` (relative to the unit height) and `strokes` — each
  // stroke is a polyline (array of [x,y] vertices) traced in order.
  var LETTERS = {
    E: {
      width: 0.62,
      strokes: [
        [[0, 0], [0.62, 0]],
        [[0, 0], [0, 1]],
        [[0, 0.5], [0.5, 0.5]],
        [[0, 1], [0.62, 1]]
      ]
    },
    S: {
      width: 0.62,
      strokes: [
        [
          [0.62, 0.13], [0.55, 0.03], [0.4, 0], [0.2, 0], [0.06, 0.05],
          [0, 0.18], [0, 0.32], [0.08, 0.43], [0.22, 0.48], [0.42, 0.52],
          [0.55, 0.57], [0.62, 0.68], [0.62, 0.82], [0.55, 0.95], [0.42, 1],
          [0.2, 1], [0.06, 0.97], [0, 0.87]
        ]
      ]
    },
    O: {
      width: 0.78,
      strokes: [
        [
          [0.39, 0], [0.55, 0.02], [0.7, 0.12], [0.78, 0.3], [0.78, 0.7],
          [0.7, 0.88], [0.55, 0.98], [0.39, 1], [0.23, 0.98], [0.08, 0.88],
          [0, 0.7], [0, 0.3], [0.08, 0.12], [0.23, 0.02], [0.39, 0]
        ]
      ]
    },
    T: {
      width: 0.66,
      strokes: [
        [[0, 0], [0.66, 0]],
        [[0.33, 0], [0.33, 1]]
      ]
    },
    R: {
      width: 0.66,
      strokes: [
        [[0, 0], [0, 1]],
        [
          [0, 0], [0.45, 0], [0.6, 0.06], [0.66, 0.18], [0.66, 0.36],
          [0.6, 0.48], [0.45, 0.54], [0, 0.54]
        ],
        [[0.38, 0.54], [0.66, 1]]
      ]
    },
    I: {
      width: 0.36,
      strokes: [
        [[0, 0], [0.36, 0]],
        [[0.18, 0], [0.18, 1]],
        [[0, 1], [0.36, 1]]
      ]
    },
    C: {
      width: 0.7,
      strokes: [
        [
          [0.7, 0.15], [0.6, 0.05], [0.45, 0], [0.25, 0], [0.1, 0.08],
          [0.02, 0.22], [0, 0.4], [0, 0.6], [0.02, 0.78], [0.1, 0.92],
          [0.25, 1], [0.45, 1], [0.6, 0.95], [0.7, 0.85]
        ]
      ]
    },
    A: {
      width: 0.78,
      strokes: [
        [[0, 1], [0.39, 0]],
        [[0.39, 0], [0.78, 1]],
        [[0.18, 0.6], [0.6, 0.6]]
      ]
    }
  };

  var WORD = ['E', 'S', 'O', 'T', 'E', 'R', 'I', 'C', 'A'];

  function samplePolyline(pts, pxPerSample) {
    var cumLen = [0];
    for (var i = 1; i < pts.length; i++) {
      var dx = pts[i][0] - pts[i - 1][0];
      var dy = pts[i][1] - pts[i - 1][1];
      cumLen.push(cumLen[i - 1] + Math.sqrt(dx * dx + dy * dy));
    }
    var totalLen = cumLen[cumLen.length - 1];
    if (totalLen === 0) return [pts[0]];

    var n = Math.max(2, Math.round(totalLen / pxPerSample) + 1);
    var out = [];
    for (var k = 0; k < n; k++) {
      var target = (k / (n - 1)) * totalLen;
      var seg = 0;
      while (seg < cumLen.length - 2 && cumLen[seg + 1] < target) seg++;
      var segLen = cumLen[seg + 1] - cumLen[seg];
      var localT = segLen > 0 ? (target - cumLen[seg]) / segLen : 0;
      out.push([
        pts[seg][0] + (pts[seg + 1][0] - pts[seg][0]) * localT,
        pts[seg][1] + (pts[seg + 1][1] - pts[seg][1]) * localT
      ]);
    }
    return out;
  }

  function buildConstellation() {
    var letterHeight = Math.min(H * 0.72, W * 0.11);
    var letterSpacing = letterHeight * 0.16;
    var pxPerSample = letterHeight * 0.085;

    var totalWidth = 0;
    for (var wi = 0; wi < WORD.length; wi++) {
      totalWidth += LETTERS[WORD[wi]].width * letterHeight;
    }
    totalWidth += (WORD.length - 1) * letterSpacing;

    var cursorX = (W - totalWidth) / 2;
    var startY = (H - letterHeight) / 2;

    var points = [];
    var edges = [];

    for (var li = 0; li < WORD.length; li++) {
      var def = LETTERS[WORD[li]];
      var letterPxWidth = def.width * letterHeight;

      for (var si = 0; si < def.strokes.length; si++) {
        var pxPts = [];
        var stroke = def.strokes[si];
        for (var pi = 0; pi < stroke.length; pi++) {
          pxPts.push([
            cursorX + stroke[pi][0] * letterHeight,
            startY + stroke[pi][1] * letterHeight
          ]);
        }

        var samples = samplePolyline(pxPts, pxPerSample);
        var firstIdx = points.length;

        for (var sp = 0; sp < samples.length; sp++) {
          points.push({ x: samples[sp][0], y: samples[sp][1] });
          if (sp > 0) {
            edges.push({ a: firstIdx + sp - 1, b: firstIdx + sp });
          }
        }
      }

      cursorX += letterPxWidth + letterSpacing;
    }

    return { points: points, edges: edges };
  }

  function init() {
    teardown();

    canvas = document.querySelector('.es-ft-starfall');
    wrap = document.querySelector('.es-ft-wordmark-wrap');
    wordmark = document.querySelector('.es-ft-wordmark');
    if (!canvas || !wrap || !wordmark) return;

    ctx = canvas.getContext('2d');
    dpr = Math.min(window.devicePixelRatio || 1, 2);
    var rect = canvas.getBoundingClientRect();
    W = rect.width;
    H = rect.height;
    if (W < 8 || H < 8) return;

    canvas.width = W * dpr;
    canvas.height = H * dpr;
    ctx.setTransform(1, 0, 0, 1, 0, 0);
    ctx.scale(dpr, dpr);

    // Reset wordmark to hidden state
    wordmark.classList.add('es-ft-wordmark--hidden');
    wordmark.classList.remove('es-ft-wordmark--revealed');

    var built = buildConstellation();
    var letterPoints = built.points;
    if (!letterPoints.length) return;

    stars = letterPoints.map(function(p) {
      return {
        trueX: p.x,
        trueY: p.y,
        currentX: Math.random() * W,
        currentY: -20 - Math.random() * H * 0.8,
        size: 0.9 + Math.random() * 1.4,
        twinkleSpeed: 0.008 + Math.random() * 0.018,
        twinklePhase: Math.random() * Math.PI * 2,
        brightness: 0.65 + Math.random() * 0.35
      };
    });

    lines = built.edges;

    if (reduce) {
      stars.forEach(function(s) {
        s.currentX = s.trueX;
        s.currentY = s.trueY;
      });
      lineAlpha = lineAlphaTarget;
      lineAlphaProxy.v = lineAlphaTarget;
      hasSettled = true;
      animationStarted = true;
      wordmark.classList.remove('es-ft-wordmark--hidden');
      wordmark.classList.add('es-ft-wordmark--revealed');
      drawFrame();
      return;
    }

    // IntersectionObserver: animate once when footer enters view
    observer = new IntersectionObserver(function(entries) {
      if (entries[0] && entries[0].isIntersecting) {
        startAnimation();
      } else if (hasSettled) {
        // Pause twinkle when offscreen to save CPU
        cancelLoop();
      } else {
        cancelLoop();
      }
    }, { rootMargin: '50px' });
    observer.observe(canvas);

    // Pre-paint frame 1 so canvas isn't blank before observer fires
    drawFrame();
  }

  function startAnimation() {
    if (typeof gsap === 'undefined') {
      stars.forEach(function(s) {
        s.currentX = s.trueX;
        s.currentY = s.trueY;
      });
      lineAlpha = lineAlphaTarget;
      lineAlphaProxy.v = lineAlphaTarget;
      hasSettled = true;
      animationStarted = true;
      wordmark.classList.remove('es-ft-wordmark--hidden');
      wordmark.classList.add('es-ft-wordmark--revealed');
      startTwinkleLoop();
      return;
    }

    if (animationStarted) {
      // Already animated once — just resume the twinkle loop
      startTwinkleLoop();
      return;
    }
    animationStarted = true;

    // Start the draw loop immediately so we see falling stars
    startTwinkleLoop();

    stars.forEach(function(s, idx) {
      gsap.to(s, {
        currentX: s.trueX,
        currentY: s.trueY,
        duration: 1.6 + Math.random() * 0.9,
        ease: 'power2.out',
        delay: 0.005 * idx + Math.random() * 0.4
      });
    });

    lineAlphaProxy.v = 0;
    lineAlpha = 0;
    gsap.to(lineAlphaProxy, {
      v: lineAlphaTarget,
      duration: 0.55,
      delay: 2.2,
      ease: 'power2.out',
      onUpdate: function() {
        lineAlpha = lineAlphaProxy.v;
      }
    });

    gsap.delayedCall(3.2, function() {
      hasSettled = true;
      wordmark.classList.remove('es-ft-wordmark--hidden');
      wordmark.classList.add('es-ft-wordmark--revealed');
    });
  }

  function startTwinkleLoop() {
    if (rafId) return;
    function tick() {
      time++;
      drawFrame();
      rafId = requestAnimationFrame(tick);
    }
    rafId = requestAnimationFrame(tick);
  }

  function drawFrame() {
    if (!ctx) return;
    ctx.clearRect(0, 0, W, H);

    if (lineAlpha > 0.001 && lines.length) {
      ctx.lineWidth = 0.8;
      ctx.lineCap = 'round';
      ctx.strokeStyle = 'rgba(' + goldR + ',' + goldG + ',' + goldB + ',1)';
      for (var li = 0; li < lines.length; li++) {
        var sa = stars[lines[li].a];
        var sb = stars[lines[li].b];
        if (!sa || !sb) continue;
        ctx.globalAlpha = lineAlpha;
        ctx.beginPath();
        ctx.moveTo(sa.currentX, sa.currentY);
        ctx.lineTo(sb.currentX, sb.currentY);
        ctx.stroke();
      }
      ctx.globalAlpha = 1;
    }

    for (var i = 0; i < stars.length; i++) {
      var s = stars[i];
      var twinkle = Math.sin(time * s.twinkleSpeed + s.twinklePhase) * 0.3 + 0.7;
      var alpha = s.brightness * twinkle;

      // Outer warm glow
      ctx.globalAlpha = alpha * 0.32;
      ctx.beginPath();
      ctx.arc(s.currentX, s.currentY, s.size * 3, 0, Math.PI * 2);
      ctx.fillStyle = 'rgba(' + goldR + ',' + goldG + ',' + goldB + ',1)';
      ctx.fill();

      // Bright cream core
      ctx.globalAlpha = alpha;
      ctx.beginPath();
      ctx.arc(s.currentX, s.currentY, s.size, 0, Math.PI * 2);
      ctx.fillStyle = 'rgba(' + textR + ',' + textG + ',' + textB + ',1)';
      ctx.fill();
    }

    ctx.globalAlpha = 1;
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }

  window.addEventListener('resize', function() {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(init, 300);
  });
})();
</script>
